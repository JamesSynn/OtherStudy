git init 등록

git add (파일이름 or *)

git commit -m "comment"

git remote add other http://github.com/Leejeeyong/OtherStudy.git 
# add 뒤에 이름과 아래 -u 뒤에 이름이 같아야 돼. ex) remote add other -> push -u other

git push -u other master

